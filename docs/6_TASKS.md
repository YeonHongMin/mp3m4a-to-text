# 🤖 TASKS - AI 개발 파트너용 프롬프트 설계서
## MP3 to Text Converter

---

## 1. 프로젝트 컨텍스트

### 1.1 프로젝트 개요
```
프로젝트: MP3 to Text Converter
목적: 로컬 환경에서 오디오 파일을 텍스트로 변환
기술 스택: Python, faster-whisper, Gradio
주요 파일:
- src/mp3_to_text.py (핵심 변환 로직)
- src/app_gui.py (Gradio GUI)
```

### 1.2 현재 기능
- ✅ CLI 변환 (단일 파일, 디렉터리)
- ✅ Gradio GUI
- ✅ 실시간 파일 저장
- ✅ 환각(hallucination) 필터링
- ✅ VAD 옵션 (활성화/비활성화)

---

## 2. 작업 프롬프트 템플릿

### 2.1 버그 수정 요청
```
[버그 수정 요청]

현상: {버그 설명}
재현 방법: {재현 단계}
예상 동작: {정상 동작}
실제 동작: {현재 버그 동작}

관련 파일: {파일 경로}
관련 함수: {함수명}
```

### 2.2 기능 추가 요청
```
[기능 추가 요청]

기능명: {기능 이름}
설명: {기능 상세 설명}
사용 시나리오: {언제, 어떻게 사용하는지}

입력: {입력 데이터/파라미터}
출력: {예상 출력}

우선순위: P0/P1/P2
```

### 2.3 리팩토링 요청
```
[리팩토링 요청]

대상: {파일 또는 함수}
현재 문제: {코드 스멜, 중복 등}
개선 방향: {원하는 구조}

제약 조건:
- 기존 API 호환성 유지
- 테스트 없이 동작 확인
```

---

## 3. 자주 사용하는 작업

### 3.1 환각 패턴 추가
```
환각 패턴 추가 요청:

새 패턴: "{새로운 환각 문구}"
위치: src/mp3_to_text.py > HALLUCINATION_PATTERNS

현재 패턴 목록에 추가해주세요.
```

### 3.2 CLI 옵션 추가
```
CLI 옵션 추가 요청:

옵션명: --{option-name}
타입: {bool/str/int}
기본값: {default}
설명: {help 텍스트}

argparse에 추가하고, 해당 로직에 연결해주세요.
```

### 3.3 GUI 컴포넌트 수정
```
GUI 수정 요청:

대상: {컴포넌트명, 예: output_text}
변경: {lines 조정, 스타일 변경 등}
파일: src/app_gui.py
```

---

## 4. 코드 스타일 지침

### 4.1 Python 스타일
```python
# 함수 정의
def function_name(param1: str, param2: int = 10) -> dict:
    """함수 설명 (한 줄).
    
    Args:
        param1: 첫 번째 파라미터 설명
        param2: 두 번째 파라미터 설명 (기본값: 10)
        
    Returns:
        반환값 설명
    """
    pass

# 클래스 정의
class ClassName:
    """클래스 설명."""
    
    def __init__(self, ...):
        """초기화."""
        pass
```

### 4.2 네이밍 규칙
| 대상 | 규칙 | 예시 |
|------|------|------|
| 함수 | snake_case | `transcribe_audio()` |
| 클래스 | PascalCase | `MP3ToTextConverter` |
| 상수 | UPPER_SNAKE | `HALLUCINATION_PATTERNS` |
| 변수 | snake_case | `audio_path` |
| 파일 | snake_case | `mp3_to_text.py` |

---

## 5. 테스트 지침

### 5.1 수동 테스트
```bash
# CLI 기본 테스트
python src/mp3_to_text.py mp3/test.mp3

# CLI 듀얼 출력 테스트
python src/mp3_to_text.py mp3/test.mp3 --dual

# GUI 테스트
python src/app_gui.py
# 브라우저에서 http://localhost:7860 접속
```

### 5.2 확인 항목
- [ ] 에러 없이 실행되는가?
- [ ] 예상 출력 파일이 생성되는가?
- [ ] 환각 문구가 필터링되는가?
- [ ] 진행률이 표시되는가?
- [ ] 중단 시 데이터가 저장되는가?

---

## 6. 일반적인 응답 형식

### 6.1 코드 수정 시
```
{수정 내용 요약}

수정 파일: {파일 경로}
수정 내용:
1. {변경 1}
2. {변경 2}

실행 방법:
{테스트 명령어}
```

### 6.2 질문 응답 시
```
{간단한 답변}

{상세 설명 (필요시)}

예시:
{코드 또는 명령어}
```

---

## 7. 금지 사항

1. **원본 오디오 파일 수정 금지**
2. **외부 API 호출 금지** (모든 처리는 로컬)
3. **기존 CLI 호환성 깨뜨리기 금지**
4. **.gitignore에 있는 파일 추적 금지**
5. **사용자 승인 없이 대규모 리팩토링 금지**

---

## 8. 유용한 컨텍스트

### 8.1 환각 필터링 위치
```python
# src/mp3_to_text.py, 라인 170~
HALLUCINATION_PATTERNS = [
    "한글자막", "자막 제작", ...
]

def is_hallucination(self, text: str) -> bool:
    ...
```

### 8.2 VAD 설정 위치
```python
# src/mp3_to_text.py, _transcribe_generator 메서드
vad_parameters=dict(
    threshold=0.05,
    min_speech_duration_ms=50,
    min_silence_duration_ms=50
)
```

### 8.3 GUI 레이아웃 위치
```python
# src/app_gui.py, 라인 190~
# CSS 및 Gradio 컴포넌트 정의
```

---

*문서 버전: 1.0*
*최종 수정: 2026-01-11*
